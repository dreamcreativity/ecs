'use strict';
angular.module('ClientApp')
.controller('RegisterCtrl',function RegisterCtrl($rootScope,$scope,$http,Courses,Students){
	loading();

	//Loaded all info in register form
	function loading(){
	}

	$scope.register = function(isValid){
	}
})